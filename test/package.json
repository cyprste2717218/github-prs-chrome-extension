{
  "name": "puppeteer-tests",
  "version": "1.0",
  "dependencies": {
    "jest": "^29.7.0",
    "puppeteer": "^23.6.0"
  },
  "scripts": {
    "test": "npm run pre-build && npm run test:e2e",
    "test:e2e": "cd e2e && jest . && cd ..",
    "pre-build": "npm run --prefix .. build"
  }
}
